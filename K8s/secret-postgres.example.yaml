# TEMPLATE — committed for reference. Do NOT put real values here.
# Create the real secret one of two ways:
#   (a) imperatively:  kubectl -n matrix create secret generic matrix-postgres-secret \
#         --from-literal=POSTGRES_PASSWORD='<pg-password>'
#   (b) sealed-secrets: kubeseal the manifest below into sealed-secret-postgres.yaml
#       and commit THAT (eloup pattern). The plaintext copy stays gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: matrix-postgres-secret
  namespace: matrix
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
